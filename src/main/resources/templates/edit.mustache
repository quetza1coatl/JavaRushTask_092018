<!DOCTYPE html>
<html>
<head>
    <title>Edit part</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <style type="text/css">
        .tg {
            /* Устанавливает, как отображать границы вокруг ячеек таблицы.
             collapse -  Линия между ячейками отображается только одна. Т.е. соседние не сливаются в одну жирную */
            border-collapse: collapse;
            /* Цвет границы */
            border-color: #ccc;

        }

        .tg td {
            font-family: Arial, sans-serif;
            font-size: 14px;
            /* Устанавливает значение полей вокруг содержимого элемента. см. подробности использования, хитро в
            зависимости от количества переданных параметров*/
            padding: 3px 2px;
            /* Устанавливает стиль границы вокруг элемента.
            [none | hidden | dotted | dashed | solid | double | groove | ridge | inset | outset]*/
            border-style: solid;
            /*Задает толщину границы. thin (2 пиксела), medium (4 пиксела) и thick (6 пикселов) или значение в px*/
            border-width: 1px;
            /*Свойство overflow управляет отображением содержания блочного элемента, если оно целиком не помещается
             и выходит за область заданных размеров. auto | hidden | scroll | visible | inherit*/
            overflow: hidden;
            /*Свойство word-break указывает, как делать перенос строк внутри слов, которые не помещаются по ширине в заданную область
            normal | break-all | keep-all*/
            word-break: normal;
            /*Определяет цвет текста элемента*/
            color: #333;


        }
        .tg th {
            font-family: Arial, sans-serif;
            font-size: 14px;
            font-weight: bold;
            padding: 3px 2px;
            border-style: solid;
            border-width: 1px;
            overflow: hidden;
            word-break: normal;

            color: #333;
            background-color: #c0d6e4;
        }


    </style>

</head>

<body>
<div>
    <table class="tg" >
        <tr width="420"><b>ТЕКУЩЕЕ ЗНАЧЕНИЕ</b></tr>
        <tr>
            <th width="250">Тип детали</th>
            <th width="120">Необходимость</th>
            <th width="50">Количество</th>
        </tr>
        <tr>

            <td>{{part.type}}</td>
            <td>{{#part.isNecessary}}да{{/part.isNecessary}}{{^part.isNecessary}}нет{{/part.isNecessary}}</td>
            <td>{{part.quantity}}</td>

        </tr>
    </table>
</div>
<div><b>ВВЕДИТЕ НОВЫЕ ЗНАЧЕНИЯ</b></div>
<div>
    <form method="post" action="update">

        <input hidden name="id" value="{{part.id}}">
        <input type="text" required="true" name="type" value="{{part.type}}" placeholder="Наименование"/>
        <input type="checkbox" name="checkbox" value="true"> Обязательно при сборке
        <input type="number" min="1" required="true" name="quantity" value="{{part.quantity}}" placeholder="Количество"/>
        <button type="submit" name="button" value="update">Изменить</button>

    </form>

    <form method="get" action="/">
        <button type="submit" name="button" value="cancel">Отмена</button>
    </form>
</div>

</body>
</html>